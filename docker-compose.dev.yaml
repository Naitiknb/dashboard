services:
  dashboard:
    build:
      context: .
      dockerfile: dockerfile.dev

    container_name: dashboard-dev

    ports:
      - "5000:5000"

    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next